#include <iostream>
#include <algorithm>
#include <string>
#include <stdio.h>
#include <string.h>
using namespace std;
string s1,s2, s3, s4;
int x, b, c, n, y ;
char pl, pl1;

//int s1.at(x);

void rs1c(string a){

    reverse(s1.begin(), s1.end());
    cout<<s1<<endl;

    char* S = new char[s1.length() + 1];
    strcpy(S, s1.c_str());

    /*for(x=0; x < s1.length(); x++){
            s2.insert(0, s1);
            while(index>=0 && S[index] !=' '){
                index--;

                hold = index -1
            }*/


            if(S[x] == ' '){
                S[x + 1] = y;
                b = s1.at(y) + 1;

                for(y = S[x+1]; x < s1.length(); x++){
                    s2.insert(0, s1);
                }

                x = 0;
            }

            //else{x++;}

        //c = S[x];
        cout << s2;

    }




int main(){
    s4 = "hello b";
    cout << "Please enter a sentence" <<endl;


    getline(cin, s1);
    rs1c(s1);


return 0;
}
